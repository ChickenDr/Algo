#include <iostream>
#include <string>
#include <vector>

using namespace std;
string word;

int main()
{
	while (1)
	{
		cin >> word;
		while (word[0] != 'a' || word[0] != 'e' || word[0] != 'i' || word[0] != 'o' || word[0] != 'u')
		{
			
		}
	}
	
	return 0;
}