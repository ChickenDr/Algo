#include <iostream>
#include <vector>
#include <cstring>
#include <algorithm>
#define ll long long
using namespace std;
ll N, M, v[2 * (10 ^ 5)], dp[];

ll solve(int dst,ll sub, bool stat)
{
    
}

int main(){
    cin >> N >> M;

    for (int i = 0; i < N; i++)
    {
        cin >> v[i];
    }
    
    return 0;
}